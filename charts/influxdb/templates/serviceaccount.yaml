# Dedicated ServiceAccount for InfluxDB — isolates pod identity
apiVersion: v1
kind: ServiceAccount
metadata:
  name: influxdb-sa
  labels:
    {{- include "influxdb.labels" . | nindent 4 }}
automountServiceAccountToken: false
