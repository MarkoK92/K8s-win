# Dedicated ServiceAccount for Ticker App — isolates pod identity
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ticker-app-sa
  labels:
    {{- include "ticker-app.labels" . | nindent 4 }}
automountServiceAccountToken: false
