# Ticker App Secrets — InfluxDB token for the API service
apiVersion: v1
kind: Secret
metadata:
  name: ticker-app-secrets
  labels:
    {{- include "ticker-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  influxdb-token: {{ .Values.influxdb.token | quote }}
