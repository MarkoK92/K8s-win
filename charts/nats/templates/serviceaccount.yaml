# Dedicated ServiceAccount for NATS — isolates pod identity
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nats-sa
  labels:
    {{- include "nats.labels" . | nindent 4 }}
automountServiceAccountToken: false
