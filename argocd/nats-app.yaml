# Argo CD Application for NATS chart
# Apply after Argo CD is installed: kubectl apply -f argocd/nats-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nats
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/MarkoK92/K8s-win.git
    targetRevision: main
    path: charts/nats
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true       # Remove resources deleted from Git
      selfHeal: true    # Re-sync if someone changes resources manually
    syncOptions:
      - CreateNamespace=true
